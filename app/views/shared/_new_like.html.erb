
    <%= form_with model: @like,  url: "/users/#{params['user_id']}/posts/#{params['id']}/likes", class: 'button_to', method: :post , local: true do |form|%>
        <%= form.submit "Like", class: 'button' %>
    <% end %>
